import PageShell from "../page-shell"

export default function DashboardPage() {
  return (
    <PageShell
      title="Overview"
      description="High-level metrics, project status, and key performance indicators for the expert network."
    />
  )
}
